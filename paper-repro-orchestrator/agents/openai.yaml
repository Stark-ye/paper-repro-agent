provider: openai-compatible
env:
  model: PAPER_REPRO_MODEL
  api_key: PAPER_REPRO_API_KEY
  base_url: PAPER_REPRO_BASE_URL
fallback_env:
  api_key: OPENAI_API_KEY
  base_url: OPENAI_BASE_URL
notes:
  - OpenAI, Qwen and other OpenAI-compatible chat APIs can be used through the same adapter.
